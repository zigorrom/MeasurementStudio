<UserControl x:Class="ChannelSwitchExecutable.ScenarioChannelSwitchView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300">
    <UserControl.Resources>
        <DataTemplate x:Key="ResourceTemplate">
            <TextBlock Text="{Binding Resource}"/>
        </DataTemplate>
    </UserControl.Resources>
    <StackPanel>
        <ComboBox x:Name="InstrumentResource" Margin="3" ItemTemplate="{StaticResource ResourceTemplate}" ItemsSource="{Binding Resources}"  SelectedItem="{Binding InstrumentResource}" VerticalContentAlignment="Center" HorizontalContentAlignment="Center"/>
        <ComboBox x:Name="ChannelSelectorComboBox" Margin="3" SelectedItem="{Binding SelectedChannel}" VerticalAlignment="Center"/>

    </StackPanel>
</UserControl>
